# Runtime dependencies for the distributable app backend (the PyInstaller
# freeze target). This is the minimal set the app needs to run.
#
# EXCLUDES (scripts/-only calibration tooling + tests — they bloat the frozen
# binary and complicate the freeze; live in requirements.txt for dev only):
#   datasets, scikit-learn, torchcodec, pytest
#
# Pinned to the exact versions validated in the dev .venv.
torch==2.12.1
torchaudio==2.11.0
transformers==5.12.1
faster-whisper==1.2.1
onnxruntime==1.27.0  # faster-whisper VAD (vad_filter=True) needs this
av==18.0.0
librosa==0.11.0
soundfile==0.14.0
praat-parselmouth==0.4.7
numpy==2.4.6
scipy==1.18.0
Pillow>=10.4  # Memorizing module: photo decode/crop (Pillow >=10.1 = numpy 2.x compat)
llama-cpp-python==0.3.34  # Qwen2.5-0.5B Q4_K_M; build with GGML_METAL=on
fastapi==0.138.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32
huggingface_hub
